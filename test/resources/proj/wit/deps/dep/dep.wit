package example:dep@0.1.0;

interface dep-iface {
    record widget {
        id: u32,
    }

    gadget: func() -> widget;
}

// A world in the dependency package, exercised by `include' path
// resolution and completion (which accept a world, not an interface).
world dep-world {
    export dep-iface;
}
